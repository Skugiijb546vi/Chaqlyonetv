name: ئامرازی پاتچکردنی خۆم بۆ مۆبایل 🚬

on:
  workflow_dispatch:

jobs:
  patch-it:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: هێنانی فایلەکانم
        uses: actions/checkout@v4

      - name: دانانی پایتۆن
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: دانانی جاڤا
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: دابەزاندنی ئامرازەکانم
        run: |
          pip install reflutter
          wget https://github.com/patrickfav/uber-apk-signer/releases/download/v1.3.0/uber-apk-signer-1.3.0.jar -O signer.jar

      - name: هێنانی ئەپەکە
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release download --pattern 'app.apk'

      - name: ئاراستەکردنی داتا بۆ مۆبایلەکەم 🚬
        run: |
          echo -e "1\n192.168.1.102\n" | reflutter app.apk
          
      - name: واژۆکردنەوە
        run: |
          java -jar signer.jar -a *RE*.apk --allowResign -o final-output
          
      - name: بەرزکردنەوەی فایلی تەواوکراو
        uses: actions/upload-artifact@v4
        with:
          name: App-Mobile-Proxy-102
          path: final-output/*.apk
